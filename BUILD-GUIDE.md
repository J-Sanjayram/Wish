# Birthday Wishes - Vite Build System

## Modern Build Setup ⚡

**Vite** - Fast build tool with hot reload and optimized production builds

## Quick Start:

1. **Install dependencies:**
   ```bash
   npm install
   ```

2. **Development server:**
   ```bash
   npm run dev
   ```
   Visit: http://localhost:3000

3. **Build for production:**
   ```bash
   npm run build
   ```
   Output: `dist/` folder

## Deployment Options:

### Option 1: Netlify
1. Build: `npm run build`
2. Deploy `dist` folder
3. Works with global JSONBin.io storage

### Option 2: Vercel
1. Connect GitHub repo
2. Build command: `npm run build`
3. Output directory: `dist`

### Option 3: GitHub Pages
1. Build: `npm run build`
2. Push `dist` contents to `gh-pages` branch
3. Enable GitHub Pages

## Features:

- ✅ **Global Storage** - JSONBin.io cloud database
- ✅ **Fast Builds** - Vite build system
- ✅ **Image Compression** - Optimized photos
- ✅ **Clean URLs** - `?wish=ID` format
- ✅ **Mobile Ready** - Responsive design
- ✅ **No Server Required** - Static deployment

## Global Storage:

- **Create Wish** → Saves to JSONBin.io cloud
- **Share Link** → `https://yoursite.com/?wish=123456789`
- **Global Access** → Works on any device worldwide
- **Image Support** → Compressed photos included

## Build Output:

```
dist/
├── index.html          # Main page
├── styles.css          # Styles
├── assets/            # Optimized assets
└── manifest.json      # PWA manifest
```

Ready for deployment to any static hosting service! 🚀